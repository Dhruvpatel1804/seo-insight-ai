version: '3.8'

services:
  web:
    build: .
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"
    env_file:
      - .env
    command: uvicorn main:app --host ${API_HOST:-0.0.0.0} --port ${API_PORT:-8000}
    volumes:
      - ./reports:/app/reports
      - ./logs:/app/logs
